/*
 * udp_packet_test.hpp
 *
 *  Created on: 24 feb. 2022
 *      Author: Pablo
 */


class udp_packet_test{
public:
	// DatagramSocket* socket;
	// double a;
	// Packet<uint16_t, uint16_t> p;

	// udp_packet_test(string myip = "192.168.1.4", int myport = 4050, string otherip = "192.168.1.3", int otherport = 4050){
	// 	 socket = new DatagramSocket(myip, myport, otherip, otherport);
	// 	 a = 123.321;
	// 	 p = {
	// 			69,
	// 			PacketValue<uint16_t>(&a, 100),
	// 			PacketValue<uint16_t>(&a, 100)
	// 	};
	// }


	void send(){
		//uint32_t aux = HAL_GetTick();
		//printf("==========[%09.0lu || Packet send!]==========\n\r\t\t\t", aux);
		// socket->send(p);
	}

};
